name: UCPA WhatsApp Scanner

on:
  schedule:
# Du lundi au samedi : toutes les heures de 5h à 23h GMT
    - cron: '*/25 5-23 * * 1-6'
# Le dimanche : toutes les heures de 5h à 11h GMT
    - cron: '*/25 5-11 * * 0'
  
  workflow_dispatch:

jobs:
  run-scanner:
    runs-on: ubuntu-latest
    environment: GEMINI_API_KEY # Lien avec tes secrets
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install requests selenium webdriver-manager
     
      - name: Run script
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GREEN_API_URL: ${{ secrets.GREEN_API_URL }}
          WHATSAPP_ID: ${{ vars.WHATSAPP_ID }}
          WHATSAPP_CHAT_ID: ${{ vars.WHATSAPP_CHAT_ID }}
          COURS_SURVEILLES: ${{ vars.COURS_SURVEILLES }} # <-- Utilise 'vars' si c'est une variable, 'secrets' si c'est un secret          EMAIL_SENDER: ${{ secrets.EMAIL_SENDER }}
          EMAIL_PASSWORD: ${{ vars.EMAIL_PASSWORD }}
          EMAIL_RECEIVER: ${{ vars.EMAIL_RECEIVER }}
          EMAIL_SENDER: ${{ vars.EMAIL_SENDER }}
          SMTP_SERVER: ${{ vars.SMTP_SERVER }}
          FREE_SMS_USER: ${{ vars.FREE_SMS_USER }} # <--- ICI
          FREE_SMS_PASS: ${{ vars.FREE_SMS_PASS }} # <--- ICI
                  
        run: python ucpa-python.py

      - name: Save memory
        run: |
          git config --global user.name 'Bot UCPA'
          git config --global user.email 'bot@ucpa.com'
          if [ -f memoire_ucpa.json ]; then
            git add memoire_ucpa.json
            git commit -m "Update memory [skip ci]" || echo "No changes"
            git push
          fi
      # 6. Sauvegarde les logs et le HTML pour debug (visible dans l'onglet Actions)
      
      - name: Upload Debug Log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ucpa-debug-log
          path: |
            debug_raw_content.txt
            execution.log
            memoire_ucpa.json
            debug_page.html
            memoire_ucpa.json
          retention-days: 2

          
